#include "main.h"

/**
* _abs - prints absolute value
* @n: integer to n=be returned
*
* Return: absolute value of n
*/

int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
